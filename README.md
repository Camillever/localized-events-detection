# Localized events detection tool

Tool to allow localised rockfall events detection with seismic signal data.

# Installation

## (Recommended) Create a virtual environment

```bash
python -m venv .env
```

## Install dependencies

```bash
pip install -r requirements.txt
```
