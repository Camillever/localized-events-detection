""" Module to calculating kurtosis - inspired by 'BackTrackBB' algorithm""""
import os

def kurtosis_backtrackbb(win:float):

    # YN1[n] = recursive_filter(y, CN_HP[n], CN_LP[n],
    #                                   filter_npoles, rmem)
    # YN1[n] /= filter_norm[n]

    # CF1[n] = recursive_hos(YN1[n], CF_decay_constant,
    #                                    hos_order, hos_sigma, rmem)

    

